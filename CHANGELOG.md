# Changelog

All notable changes to this project are documented here. Format loosely
follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), versions
follow [SemVer](https://semver.org/). This project is pre-1.0 (alpha) —
expect breaking changes between minor versions. Version bumps and this file
are generated by [commit-and-tag-version](https://github.com/absolute-version/commit-and-tag-version)
(`npm run release`) from Conventional Commits — see `CONTRIBUTING.md`.

## [Unreleased]

### Changed
- **Performance/UX: most action buttons no longer trigger a full refetch
  after their own mutation.** ~56 of the ~63 mutate-then-`load()` sites
  across every dashboard now merge the mutation's own API response
  directly into local state instead — a click that used to fire two
  network round trips (the mutation, then a refetch of everything the
  view needs) now fires one. Every conversion was checked against the
  actual API route's real response shape first (not assumed from the
  function name); ~7 sites were deliberately left on the refetch pattern
  because their route's response doesn't contain enough to reconstruct
  correct UI state locally (`trial-enroll`'s addService, `convert`'s
  Student-path invoice reassignment, and both invoices/paychecks
  `action=generate` bulk endpoints, which return a flat unlinked array) —
  listed in the commit for anyone touching those routes later. Verified
  live: closing a ticket no longer fires a second `GET /api/tickets`, and
  the closed state renders correctly from the mutation response alone.

### Added
- **Loading/disabled states on action buttons.** Only 3 of 135 `onClick`
  handlers across the dashboards gated on any busy state — every other
  async action button (Approve/Reject, Mark as received, Save, Delete,
  etc.) stayed fully clickable during its own network round trip, with no
  visual feedback and a real double-submit risk on a fast double-click.
  Row-scoped actions (e.g. per-row Approve/Reject in a table) use a
  per-id busy Set so one row's action doesn't disable every other row;
  single-action components use a local `saving`/`busy` boolean. Found and
  fixed one pre-existing asymmetric gap along the way: `TicketRow`'s
  Reopen button had no busy gating even though its sibling Save/Close
  buttons did. Verified live: a ticket's Reopen button correctly shows
  "Reopening…" and disables itself for the duration of the request.

### Fixed
- **Critical: concurrent writes to the same collection could silently
  delete each other's records.** `sync_table()` (used by every `writeDB()`
  call) deleted every row NOT in the calling request's in-memory snapshot
  before upserting — two concurrent requests writing the same collection
  each read the table before the other's insert landed, so each snapshot
  excluded the other's new row, and whichever write ran last deleted the
  other's record. Reproduced live: 8 concurrent ticket creations left only
  1 of 8 behind. Fixed with a new Postgres function, `upsert_rows()`
  (insert/update only, never deletes — data/tmp/migration_incremental_writes.sql,
  applied to production), plus a new `deleteRecords()` export for the
  handful of routes that actually remove records (apikeys, services,
  enrollments, invoices, paychecks, and both migrate-monthly merges),
  which now target only the exact ids being removed instead of relying on
  writeDB to infer deletions from what's missing off a snapshot. Verified
  live: the same 8-concurrent-request test now leaves all 8 records
  intact; a mint-then-delete round trip on a real record confirmed the
  explicit delete path still actually removes it from Postgres.
- **Id minting raced under concurrency too, hitting production twice**
  (paychecks, attendanceitems — Sentry: "ON CONFLICT DO UPDATE command
  cannot affect row a second time"). `nextId()` incremented `db.counters`
  purely in memory, so two concurrent requests could read the same
  counter value before either wrote it back and both mint the identical
  id. Fixed with a new Postgres function, `increment_counter()`
  (data/tmp/migration_atomic_counter.sql, applied to production) — the
  read-and-increment now happens as one atomic UPDATE, so concurrent
  callers are serialized by the database itself instead of racing on
  JS-level state two separate serverless invocations can never share.
  Every `nextId()` call site across `app/api` was updated to `await` it.
  Verified live: 8 concurrent requests now mint 8 distinct ids with zero
  conflicts. Found via a Sentry review that also surfaced the
  collection-wipe bug above — both share the same root cause (deriving a
  write from a stale in-memory snapshot instead of a database-atomic
  operation).

### Changed
- **Performance: `writeDB()` now only syncs the collection(s) a route
  actually mutated**, instead of unconditionally re-syncing all 16
  Supabase-backed tables (a real network round trip per table) on every
  single save. `writeDB(db, touchedCollections)` takes an optional array
  of collection keys; every one of the 27 API route files that write to
  the DB was updated to pass exactly what it touches, verified against
  each mutation site individually (no call site was left unscoped —
  guessing wrong here would risk silently dropping data, so anywhere the
  touched set wasn't obvious from the route's own code was checked against
  its actual mutations, not assumed). Omitting the argument keeps syncing
  everything, so nothing outside `app/api/**` needs to change. Confirmed
  live: a single-collection write (mint/delete an API key) leaves every
  other table's record count unchanged. Biggest single latency win in the
  app — most button clicks that save something now do 1 table round trip
  instead of up to 16.
- **Calendar cells (`ScheduleCalendar`)**: a day cell with many sessions
  used to grow unbounded, breaking the week row's height and making a
  busy month's calendar extremely tall. Cells now have a fixed max height;
  once a day has more than 3 sessions the rest collapse into a "+N more"
  pill. Clicking a cell (or its "+N more" pill) opens a popover showing
  every session for that day in full, with the exact same click-to-expand
  attendance/roster behavior each chip already had. The week grid itself
  is now a fixed-height, vertically-scrollable region inside the calendar
  card (nav/legend stay fixed above it) instead of growing the whole page
  — applies to both calendar boxes in Management's Schedule tab (Open
  pool slots and Service Schedule).
- **TKT-0041 follow-up**: the Teacher Schedule Image's "Batch:" value
  (added earlier in TKT-0041, derived from the teacher's enrolled Batch)
  is removed again per direct request — a teacher can be enrolled across
  multiple batches at once, so showing any single batch name there would
  misrepresent the rest. The "Batch:" label stays on the template (baked
  into the image asset) but its value is now always blank for Teacher
  accounts; Staff's Department value is unaffected.

### Fixed
- **TKT-0041**: A Teacher's weekly Schedule Image "Batch:" field could be
  blank even for a genuinely enrolled teacher — it read a free-text
  `user.Batch` field Management had to type by hand on the account,
  completely disconnected from the teacher's real enrollment data, so any
  teacher enrolled without someone also manually duplicating the batch
  name onto their profile showed blank (live case: TCH-0005 "zahra",
  enrolled in Batch B14 via ENR-0051, showed no Batch on her image).
  `/api/schedule/image` now derives the label from the teacher's actual
  enrollments/BatchID — same source of truth the schedule grid itself
  uses — falling back to the old manual field only if no enrollment batch
  is found. Not a regression of TKT-0008 (that fix still works exactly as
  before for accounts that DO have the manual field set); this closes the
  underlying data gap that caused it.
- **TKT-0018**: The old Interview slot picker (removed in TKT-0021 — a
  candidate no longer chooses a time, just requests, Management assigns
  the actual slot on approval) is gone, but once a slot WAS assigned the
  "My Interview" status line still showed `with {Facilitator}` — the
  interviewer's name, leaked to the candidate ahead of the interview,
  which is exactly what this ticket asked to remove. Also fixes a
  dangling-space bug this caused whenever Facilitator happened to be
  blank (a real live case: SCH-1792 currently has no Facilitator set,
  which rendered as "...at 16:00 with OfferAccepted" — a stray trailing
  "with" running straight into the status badge).
- **TKT-0023**: A wrong password on login showed no error at all — the page
  just silently reloaded itself with the form cleared. Root cause: the
  shared `api()` helper's global 401-handler (meant to bounce an
  expired/missing session cookie back to `/login`) also caught
  `/api/login`'s own 401 (wrong username/password) — wiped localStorage,
  force-navigated to `/login`, and returned a `Promise` that never
  resolves, so the login form's `catch(err)` never ran and
  "Invalid username or password." was never shown. `/api/login` is now
  exempted from that branch — its 401 means "wrong credentials," not
  "your session died." Verified live: a wrong-password attempt now shows
  the real error message in place, with the username field still filled
  (no reload); confirmed a genuinely expired session on every other
  endpoint still correctly bounces to `/login` as before.
- **TKT-0047**: Repo-wide pass for form controls with no real `<label>` —
  same bug class as the Enrollments Service/Batch/Rate selects (fixed
  separately, commit 12a92a5). Fixed: the account-creation form's Currency
  select (silently auto-selected the first currency, zero indicator);
  `MiniAttendanceForm`/`AttendanceForm`'s Status select and Hours input —
  three separate copies of the same unlabeled widget
  (`components/ScheduleCalendar.jsx`, `app/dashboard/staff/page.js`,
  `app/dashboard/ambassador/page.js`) used across every Teacher/Student/
  Staff/Ambassador attendance-logging surface; the "Offer a Trial/
  Interview Slot" form's slot-type select; the Services form's Staff-role
  Department select; both Rates-row Currency/BillingType selects (flat and
  nested-Batch forms — Rate/Description already had placeholders, only
  these two didn't); and three bare date+time input pairs with zero label
  (`components/RescheduleControl.jsx`'s self-service reschedule form,
  Management's `RescheduleCell` direct-reschedule form, and
  `InterviewSlotAssign`'s "offer a new slot" form). Verified live via
  Playwright against the real dev server — every label renders correctly,
  no layout regressions.
- **TKT-0028**: Occurrence's Facilitator was purely free text, typed once
  when a Batch was created and never kept in sync with reality — a Teacher
  enrolled after the fact (or a copy-pasted Batch) could show a
  permanently stale name. Real case: Ahmer's own enrolled class
  (SVC-0004/BATCH-0002) showed a different Teacher's name because that's
  what was typed in before Ahmer was enrolled there. New optional
  `Occurrence.FacilitatorUserID` — when linked to a real Teacher account,
  `Facilitator` (the display string every read site already uses,
  unchanged) is re-derived from that account's current Name on every
  Service save, so it can't drift stale again. Left unlinked, Facilitator
  stays exactly as before: plain free text, still needed for a guest/
  placeholder instructor with no account (confirmed real cases exist —
  this couldn't become a hard requirement to link an account).
  Management's Occurrence editor (both the cohort/Batch form and the
  role-based flat form) gets a new optional 🔗 link-to-Teacher picker
  alongside the existing free-text input. Fixed Ahmer's real data live
  (BATCH-0002's two occurrences now correctly show "Ahmer", linked to
  TCH-0004) and verified the self-healing sync end-to-end: renamed the
  linked account, re-saved the Service, Facilitator auto-updated with
  zero manual re-entry.
- **TKT-0022**: `POST /api/paychecks` (`action: "generate"`) 500'd generating
  August's draft — traced to `counters.PAY` having drifted behind the
  highest `PaycheckID` actually in use (14 vs 18 real). `nextId()` minted
  an id that already existed; Postgres's batched upsert then rejected the
  whole write ("ON CONFLICT DO UPDATE command cannot affect row a second
  time" — a confusing generic error with no hint at the real cause).
  `nextId()` (`lib/db.js`) now self-heals: the first time a given prefix
  is minted in a request, it verifies the stored counter against actual
  data (a full-tree scan, top-level ids and nested ones alike) and uses
  whichever is higher — cached after that first check so a loop minting
  the same prefix repeatedly (like paycheck generation) doesn't re-scan
  per call. Fixes the drift for every id prefix, not just PAY, and
  self-corrects going forward regardless of what caused a given drift.
- **TKT-0044**: Billing's "Generate Drafts" help text claimed "Only INR
  Amount and INR Due need manual entry" — stale and factually wrong.
  INR Amount auto-converts using the exchange rate as of the 1st of the
  invoice/paycheck's own month (already implemented, verified working
  against real invoices/paychecks) and has no manual-entry UI at all;
  only INR Due is genuinely editable, for partial-payment tracking. Text
  now describes actual behavior.
- **TKT-0035**: A student who never logged their own attendance for a
  class that happened previously produced a $0 invoice line for that
  subject — even though the class actually ran. `computeHoursAndAmount()`
  (`lib/billing.js`) now takes `assumePresentIfUnlogged`: a scheduled
  session with literally no attendance record from anyone (deliberately
  asymmetric, not a bug — Students are billed for a class that happened
  even if nobody logged it; Staff are still only paid for hours someone
  actually confirmed) counts as fully attended. Invoices pass `true`,
  Paychecks pass `false` (matching prior behavior exactly, no change on
  the Staff side). A session with at least one real record — from either
  party, see TKT-0037 — is completely unaffected either way. The other
  half of this ticket (a student who forgot but the co-enrolled Teacher
  logged for them) was already fixed by TKT-0037's fallback mechanism.
- **TKT-0027**: Every dashboard's schedule/attendance List view
  (Teacher, Student, Staff, Ambassador, Parent, and Management's Service
  Schedule) showed every session ever generated with no date filter at
  all, oldest first — past sessions dominated the default view. Added a
  "Show past" checkbox (unchecked by default) to each — today's own
  sessions still show, since they still need logging. New
  `todayDateStr()` helper (`lib/client.js`). Calendar views were already
  correct (they scope to one month via their own navigation) and are
  untouched; Management's Calendar view for Service Schedule is
  similarly untouched, only its List view is filtered.
- **TKT-0030**: Billing's Paychecks and Invoices tables were one flat list
  each, sortable by column but with every person mixed together — read as
  if Teacher paychecks weren't showing at all, when they were just
  unlabeled among Staff/Ambassador ones under one "(Staff)" heading.
  Paychecks now group by Type (Teachers / Staff / Ambassadors) then by
  Person; Invoices group by Person (every invoice holder is a Student,
  so no Type level needed there). Each person's paychecks/invoices are
  collapsed by default, click to expand — also directly helps the
  standing "too much forced scrolling" complaint (TKT-0020/0042): the
  whole Billing tab now fits on one screen collapsed instead of a
  multi-thousand-pixel scroll. Column-sorting (previously per-column,
  via SortableTh) is dropped in favor of the new fixed group order
  (Type → Person → Period-descending) — a deliberate trade, not an
  oversight.

### Changed
- **TKT-0003**: Invoices are now billed month-wise, not subject-wise — one
  combined Invoice per student per month (Monthly/Hourly-billed subjects),
  with an itemized LineItems breakdown per subject. OneOff-billed invoices
  (books, admissions, etc.) are unaffected — still one standalone invoice
  ever, unrelated to a specific month.
- Invoice totals shown to Students/Parents are now in the student's own
  profile currency (converted + locked at generation, same convention as
  the existing INR conversion); Management's Billing view still shows an
  INR column for internal accounting.
- Mid-month enrollment start/end dates now prorate billed hours correctly
  (previously an enrollment starting partway through a month was billed
  for the whole month's scheduled hours).
- Paychecks now mirror the same month-wise billing as Invoices — one
  combined Paycheck per staff member per month (Monthly/Hourly-billed
  services), itemized LineItems breakdown, OneOff paychecks unaffected.
  Payslip PDFs are itemized per subject with correct Gross/Nett Pay and
  Employer SOCSO across the combined total. New
  `POST /api/paychecks/migrate-monthly` (same dry-run-by-default,
  backup-snapshot pattern as the invoice migration) folds existing
  per-subject paychecks into the new shape. Also fixes a pre-existing
  bug where a freshly generated paycheck's INR Due was always 0 instead
  of the full amount owed (Amount Due showed "nothing owed" on a
  brand-new unpaid paycheck).
- Invoice PDFs are now one per student per month, itemized per subject.
- **TKT-0013**: Manual invoice creation is now month-wise, not one service
  at a time — pick a student and month, get a checklist of every subject
  they're currently enrolled in (all pre-checked by default), type an
  amount per checked subject, submit once to create/append all of them.

### Added
- **TKT-0033**: Track when things were actually sent/received, not just
  their current status. New timestamp fields, stamped the first time each
  transition happens (never overwritten by a later no-op edit, and cleared
  back out if the underlying flag is ever unset): Invoices get `SentAt`
  (Status → Sent) and `PaidAt` (`StudentPaidFlag` → true, both the proof-
  upload path and the self-report path); Paychecks get `SentAt` and
  `ReceivedAt` (`StaffReceivedFlag` → true); Interview offers get
  `OfferSentAt` and `OfferAcceptedAt` (`OfferSentAt` clears on "unsend",
  symmetric with the others). Reschedule requests get `ReviewedAt`
  (stamped once on approve or reject — no clearing needed there, a
  decided request can't revert to Pending). Shown as small dates under
  the existing Status/Paid/Received badges in Management's Billing and
  Interview Pipeline tables; reschedule requests' `ReviewedAt` has no new
  UI surface of its own since resolved requests never appear in any
  current view — already visible via the existing Audit Log, which logs
  every approve/reject with its own timestamp.
- **TKT-0029**: New "Pending Reschedule Requests" panel at the top of
  Management's Schedule tab, listing every pending self-service reschedule
  request in one place with Service, requester, current vs. requested
  date/time, and Approve/Reject actions — previously a pending request was
  only ever visible on its own row, inside whichever Calendar/List view you
  happened to have open for that specific slot, with no way to see
  everything currently awaiting a decision at once. Reuses the existing
  `GET`/`PATCH /api/schedule/reschedule-requests` (already fetched by this
  page, already Management-only, already joined with requester name and
  full slot data) — no new backend, only a new place to see and act on it.
  Hidden entirely when there's nothing pending.
- **TKT-0037**: Teachers can now log attendance for their co-enrolled
  Students, and Students can log it for their co-enrolled Teacher (self-
  logging unchanged for everyone else) — expandable per-session roster
  (`components/SessionAttendance.jsx`) on Teacher/Student calendar+list
  views and the admin Schedule tab's Service Schedule list/calendar.
  More than one person's attendance record can now coexist for the same
  (session, subject) — both kept forever as history, `AcceptedForBilling`
  marks which one billing reads (self-record wins by default; falls back
  to another author's record only if the subject never logged their own —
  this is what actually fixes the "$0 invoice because student forgot to
  log" case). A genuine, unreviewed disagreement blocks that specific
  invoice/paycheck from being marked Sent (never blocks Draft generation)
  until Management picks which record is correct via a new "Mark correct"
  action (`PATCH /api/attendance`) — flips acceptance, stamps
  `ResolvedBy`/`ResolvedAt`, deletes neither record.
  `POST /api/invoices` and `POST /api/paychecks`'s manual-creation path
  now hard-blocks creating a second invoice/paycheck for a person+month
  that already has one (previously silently appended a line item) —
  the multi-subject "check several, submit once" form now sends all
  checked subjects as one atomic `lineItems` array so they still land
  together, rather than each becoming its own now-rejected request.
  Also fixed, found while building this: legacy ScheduleItems with no
  `BatchID` recorded (~184 of 592 in current data) were silently failing
  every enrollment/attendance lookup that matched on Batch — a Teacher
  could get "You're not enrolled" on their own real session.
- **TKT-0026**: The enrollment form's Batch picker now shows each Batch's
  own Day/Time schedule alongside its name (e.g. "Batch A (Morning) —
  Mon 09:00, Wed 09:00") — previously only `BatchName` (which can be
  generic or blank) was shown, leaving Management to pick a Batch blind.
  `batchScheduleLabel()` (`lib/billing.js`). Choosing a specific Batch
  itself already worked (shown once a Service has more than one) — the
  gap was visibility into what each option actually meant.
- The Enrollments tab's Service/Batch/Rate selects now each have a real
  `<label>` — previously unlabeled, relying only on a placeholder option
  ("Select service…") that the Batch and Rate selects didn't even have
  once a Service was picked (both auto-select their first option
  immediately). TKT-0047 tracks doing the same across the rest of the
  app — this was one specific instance, not a full sweep.
- Management can now edit a ticket's message after creation, from the
  admin Tickets tab — previously the only mutations were close/reopen.
  `PATCH /api/tickets` takes a new explicit `action: "edit"` (separate
  from the mere presence of a `message` field, so an edit request can
  never accidentally close/reopen a ticket as a side effect, or vice
  versa) — allowed on a closed ticket too, since fixing a typo shouldn't
  require reopening it first.
- Closing a ticket can now include an optional resolution note
  (`CloseMessage`) — shown under the Closed date in the admin Tickets
  tab. Can be added/edited even after the ticket is already closed
  (re-sending `action: "close"` with a new `closeMessage` updates the
  note without touching `ClosedAt`/`ClosedBy` again), and clears on
  reopen since it no longer applies once the ticket's active again.
- **TKT-0021/TKT-0016**: Interview accounts (Teacher/Staff/Ambassador
  tracks) no longer pick their own slot — they request an interview
  against a Service, and Management assigns the actual slot (existing
  open one, or a brand-new one created on the spot) when approving the
  request. Removed the old "Available Interview Slots" picker entirely
  (`availableInterviewSlots` dropped from `/api/me`); `PATCH
  /api/schedule/requests` now takes a `scheduleId` when approving an
  Interview request, validated against the request's own Service and
  against double-booking. Also resolves TKT-0016 by construction — past
  dates never entered the candidate-facing flow in the first place, and
  Management's own slot picker only offers today-or-later.
- **TKT-0015**: The Enrollments tab's enroll form can now mint a one-off
  custom Rate inline, per service row ("+ New rate for this service"),
  instead of being limited to a Service's already-defined Rates — new
  `POST /api/services/rates` appends one Rate to an existing Batch (or a
  role-based Service's own Rates list) without resending the whole Service.
- Ticket # is now shown as its own column on the admin Tickets tab.
- The Worksheets resource page now auto-lists a Google Drive folder's
  contents as clickable cards (each opening its own file directly) when
  WorksheetsLink is a Drive folder link, alongside the existing "Access
  Worksheets" button — new `GET /api/resources/drive-folder`. Requires
  `GOOGLE_DRIVE_API_KEY` to be set; the target folder must be shared
  "Anyone with the link", same assumption the existing link already makes.
- `POST /api/invoices/migrate-monthly` — one-time migration folding
  existing per-subject invoices into the new combined-per-month shape.
  Dry-run by default (`{ apply: true }` required to write); dry-run
  response includes a full pre-migration backup snapshot.
- **TKT-0004**: Student accounts now have an optional Parent Email field
  (Management-only edit), alongside the existing Parent WhatsApp Number.
- **TKT-0005**: Bulk-enroll a single student into several services at once
  from the Enrollments tab, sharing one Start/End date across the batch.
  Redesigned into the existing single-enroll form itself (repeatable
  "+ Add another service" rows, each with its own Batch/Rate picker)
  instead of a separate Bulk Enroll box.
- **TKT-0006**: Search box on the Services tab, filtering by name.
- **TKT-0007**: Standardized all date displays across the app to
  DD/MM/YYYY (DD/MM/YYYY, HH:MM for timestamps) via a shared
  `lib/formatDate.js` utility — schedule/attendance tables on every
  dashboard, reschedule requests, enrollment Start/End dates, audit
  log and ticket timestamps, and invoice/paycheck PDFs. Date *inputs*
  are unaffected (native `<input type="date">` is always internally
  yyyy-mm-dd regardless of display format) — this is display-only.
- **TKT-0008**: Fixed the Schedule Image — Student header now shows the
  student's own Course field (e.g. "IGCSE") instead of a blank slot.
  Each occurrence's Day/Time is now converted from the timezone it was
  actually scheduled in into the viewing user's own profile timezone,
  so displayed times genuinely match the timezone printed in the
  header — previously the raw stored time was shown as-is, mislabeled
  under the viewer's timezone even when the occurrence was set in a
  different one (`lib/timezones.js`'s new `convertWeeklyTime`, using
  `Intl.DateTimeFormat` — correctly handles DST).

### Fixed
- `allowedDevOrigins` now also matches `*.trycloudflare.com` — viewing the
  dev server through a `cloudflared tunnel --url` quick tunnel had Next.js
  blocking cross-origin dev requests, so client JS never hydrated. SSR
  HTML still rendered fine, but anything gated behind a client-only mount
  (Nav — hidden until mounted, to avoid a theme/scroll hydration mismatch)
  stayed permanently invisible, making the nav bar appear "missing" on the
  home page when viewed through the tunnel.
- Code review finding: `generate` could create a duplicate invoice for a
  student/month that was already billed under the pre-rewrite flat shape
  but hadn't gone through `migrate-monthly` yet — now skipped, same as
  any other already-generated line item.
- Management's Billing view now shows a non-blocking warning (⚠ dup?)
  on any student/month with more than one invoice, with a recommendation
  to delete the duplicate if it's genuinely double-billing the same
  subjects — doesn't block creation, since a real OneOff purchase can
  legitimately coexist with a combined invoice in the same month.
- Schedule Image: every grid cell was filled with the role's color
  (blue/green/amber) regardless of whether a class was actually
  scheduled there, making an empty week look identical to a fully
  booked one. Now only cells with an actual class are colored;
  everything else is plain white. Found during manual testing of
  TKT-0008 (`lib/scheduleImage.js`'s `drawSchedule`, unrelated to the
  earlier label/timezone fix in that same ticket).

## [0.1.1](https://github.com/funteck123/divergencie-web/compare/v0.1.0...v0.1.1) (2026-07-30)

### Added
- Automated versioning/changelog via `commit-and-tag-version` (`npm run
  release`) — computes the next SemVer version and rewrites this file from
  Conventional Commits, commits and tags locally, but never pushes; push
  stays a deliberate manual step since it triggers Vercel's production deploy.
- Error monitoring via Sentry (server + client instrumentation), tuned for
  free-tier usage: low trace sampling (5%), 404s and browser-extension noise
  filtered out before they count against quota.
- Management audit trail (`auditlog` table) — every mutating admin action
  (services, users, invoices, paychecks, enrollments, reschedule approvals,
  API keys, ticket close/reopen) now records who did what, when.
- App-level structured logging (`applogs` table) alongside the audit trail.
- Generic issue-reporting Tickets feature: any signed-in account can raise a
  ticket (message + optional attachment URL) via a "Report an issue" button
  on every dashboard; Management can view, close, and reopen tickets from a
  new Tickets tab.

### Fixed
- `$0`-total invoice warning now only fires when it's actually misleading.
- Reschedule-request approval closed an IDOR (a non-owner could act on
  someone else's request).

### Notes
- This release also ships the full backlog of commits from Jul 19 onward
  that had never been pushed to `origin/main` before now — see `git log`
  for the complete list; only the changes above were made in the session
  that added this changelog and the tickets/monitoring features.
