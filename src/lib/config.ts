/**
 * DivergenCIE Application Configuration
 *
 * PERFORMANCE_MODE: Set to true to disable high-end animations and transitions.
 */
export const APP_CONFIG = {
  PERFORMANCE_MODE: false,
};

/**
 * Business constants — edit here, reflected across the whole app.
 */
export const BUSINESS = {
  /** Commission paid per successfully converted referral (GBP) */
  AMBASSADOR_COMMISSION_PER_REFERRAL: 25,
  /** Fixed monthly allowance for Tier 1 ambassadors (GBP) */
  AMBASSADOR_TIER1_ALLOWANCE: 150,
  /** Default hourly rate for teachers without a set rate (GBP) */
  DEFAULT_TEACHER_HOURLY_RATE: 20,
};
